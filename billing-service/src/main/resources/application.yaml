spring:
  application:
    name: billing-service
  datasource:
    url: jdbc:h2:mem:billing-db
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop

server:
  port: 8083